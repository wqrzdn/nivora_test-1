// Direct import of the UserProfile component
// This approach avoids the need for a separate wrapper component
import UserProfile from '../components/profile/UserProfile';

export default UserProfile;
